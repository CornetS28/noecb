const styles = (theme) => ({
  archivesWrapper: {
    flexGrow: 1,
    backgroundColor: theme.palette.primary.main,
  },
  archivesContainer: {
    margin: "auto",
    minHeight: "59.3vh",
  },
  test: {
    color: theme.palette.color.white,
  },
});

export default styles;
