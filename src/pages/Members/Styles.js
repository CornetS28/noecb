const styles = (theme) => ({
  membersWrapper: {
    flexGrow: 1,
    backgroundColor: theme.palette.primary.main,
  },
  membersContainer: {
    margin: "auto",
    minHeight: "59.3vh",
  },
  test: {
    color: theme.palette.color.white,
  },
});

export default styles;
