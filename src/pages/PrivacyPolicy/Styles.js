const styles = (theme) => ({
  privacyAndPolicyWrapper: {
    flexGrow: 1,
    backgroundColor: theme.palette.primary.main,
  },
  privacyAndPolicyContainer: {
    margin: "auto",
    minHeight: "59.3vh",
  },
  test: {
    color: theme.palette.color.white,
  },
});

export default styles;
