const styles = (theme) => ({
  articlesWrapper: {
    flexGrow: 1,
    backgroundColor: theme.palette.primary.main,
  },
  articlesContainer: {
    margin: "auto",
    minHeight: "59.3vh",
  },
  test: {
    color: theme.palette.color.white,
  },
});

export default styles;
